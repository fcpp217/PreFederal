"""Play-by-play a partir de un relato de voz.

El relator dice frases cortas con una gramática fija (ver `vocabulario`),
el audio se transcribe (`transcripcion`), se convierte en eventos
(`parser`) y con esos eventos se arman las estadísticas (`estadisticas`).
Es una fuente independiente del play-by-play oficial de la API.
"""
